import React from 'react';

const Header = () => (
  <header style={{ textAlign: 'center', color: '#fff', height: 64, paddingInline: 48, lineHeight: '64px', backgroundColor: '#4096ff' }}>
    Header
  </header>
);

export default Header;